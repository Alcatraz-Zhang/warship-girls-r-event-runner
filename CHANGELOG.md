# 变更记录

## 0.1.1 - 2026-08-18

- 将 Codex 可选安装示例更新为当前个人级 `$HOME/.agents/skills` 和仓库级 `.agents/skills` 目录；
- 将 GitHub Actions 的 `actions/checkout` 升级并固定到 v7.0.1 提交，消除旧 Node.js 运行时提示。

## 0.1.0 - 2026-08-17

- 发布与具体 Agent 产品无关的中文活动执行流程，覆盖当期资料研究、自动带路、编队、战斗结算与中断恢复；
- 提供动态解析 MuMu/ADB 目标、生成唯一截图证据、限时运行外部进程、比较实时画面和发送单次证据绑定输入的 PowerShell 脚本；
- 提供可复制到任务工作区的 JSON 检查点模板；
- 补充中文首页、入口元数据、参考资料、贡献规范、安全报告和评测说明；
- 加固 MuMu 目标索引、启动 ID 别名、游戏 PID、前台 Activity、保守捕获时间和 KeyEvent 白名单校验；
- 严格校验截图证据侧车的字段类型，并在解析实时目标前拒绝缺失参数、越界坐标和非法包名，同时兼容采集器合法产生的空索引、空启动 ID、空覆盖分辨率与空旋转值；
- 在每次输入前连续抓取实时 A/B 画面并再次解析目标，以固定 `64 × 36` RGB/梯度算法遮罩小范围动画，同时守卫全局页面和点击、滑动或按键位置；视觉检查开始前即原子消费证据，实时 B 帧超过 3 秒即失败关闭，失败时在回执中保留固定门限与判定指标；
- 将评测场景中的具体舰名和路线中性化为明确的虚构占位符，并澄清汇总分数来自发布前旧标签版本的一次历史运行；
- 新增仓库验证器回归测试，并把静态验证、验证器回归、MuMu 安全和输入助手四项离线验证纳入本地说明与 CI；
- 加入仓库结构、JSON、PowerShell 语法、相对链接、隐私与禁入文件的离线验证，并以假 CLI、假 ADB 和随机本机监听端口覆盖目标解析、截图取证、证据绑定输入三个接口；
- 经 MuMu 12 实机验收后，补充 `dumpsys window displays` 前台识别回退，并让测试进程在 PowerShell 7 与 Windows PowerShell 5.1 下都调用当前解释器。
