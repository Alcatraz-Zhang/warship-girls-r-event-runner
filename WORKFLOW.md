# 战舰少女R活动执行工作流

本文是与具体智能体产品无关的操作规范。执行器可以使用不同的浏览器、截图查看器或命令运行能力，但必须遵守同一条主循环：**动态确认目标，取得唯一的新截图并识别页面，只依据这份证据执行一次输入，再重新截图。**

本目录提供的五个 PowerShell 脚本是 Windows 与 MuMu Player 12 环境下对这条主循环的参考实现；流程本身不依赖某个对话平台、任务系统或安装路径。

## 能力接口

执行器在开始前应确认具备以下最小能力，并把实际工具映射到这些抽象接口：

| 接口 | 输入 | 输出或保证 |
|---|---|---|
| 目标解析 | 可选的 `<VM索引>`、游戏包名、是否要求前台 | 唯一的 MuMu 虚拟机、当前 ADB 串号以及设备与游戏指纹；有歧义时失败 |
| 状态取证 | 目标、`<会话证据目录>` | 唯一命名的截图和同名 JSON 侧车；侧车包含尺寸、哈希、时间与指纹 |
| 画面识别 | 最新截图 | 当前页面、可见控件、风险提示和下一项候选动作；不能以旧文字记录代替画面 |
| 证据绑定输入 | 一份未消费的侧车和一个点击、短滑动或允许的按键事件 | 输入前重新核对目标与证据，原子消费侧车，并留下回执 |
| 当期资料检索 | 活动名称、日期、版本、关卡或题目原文 | 带来源与更新时间的路线、题库或机制结论，以及仍未解决的不确定性 |
| 检查点读写 | 当前任务工作区内的检查点 | 可恢复的已核验状态；不得写回已安装的工作流目录 |
| 用户协作 | 歧义、不可逆操作或权限缺口 | 暂停并提出精确问题，不以猜测代替授权或证据 |

只读研究和本地检查点属于流程内部能力。向论坛发帖、点赞、收藏、关注、修改云端文档或发送消息等外部写入，不在默认能力范围内。

## 建立或恢复会话

1. 明确用户要完成的范围和完成标准，例如首通、指定章节、活动题库、战况中心、宝箱、奖励或功勋。用户说“继续”时，应恢复现场，不要求用户凭记忆重述页面。
2. 从 [检查点模板](assets/checkpoint-template.json) 在当前任务工作区建立会话检查点；记录活动、目标范围、完成定义和已知 `<VM索引>`。不要把账号进度存入本仓库或已安装技能目录。
3. 在启动时动态探测 MuMu 虚拟机和本次启动的 ADB 端口；电脑、模拟器、游戏或图形驱动重启后必须重新探测。不得沿用历史端口。
4. 若检查点有可信的 `<VM索引>`，优先要求解析器按该索引查询 MuMu 命令行的实时信息。配置文件、当前启动日志、已连接的回环 ADB 串号和有限端口探测只作为后备来源。
5. 只有格式规范且非零的 `BootId` 才能证明多个串号属于同一次 Android 启动并据此合并别名；`BootId` 缺失、无效或全零时，各串号必须保持独立。`AndroidId` 和设备型号只用于诊断，不能作为合并依据。优先采用 MuMu 命令行报告的实时端口；若有多个不同虚拟机都运行目标游戏且未给定索引，立即暂停并询问，绝不选择列表中的第一个。
6. 输入前必须确认 ADB 在线、目标是回环地址、游戏包与 PID 存在且游戏处于前台。只在查找尚未启动的游戏时使用宽松发现模式，不能据此点击。
7. 记录完整指纹：`VM索引`、`BootId`、`AndroidId`、游戏 PID、前台包名与活动名、物理分辨率、覆盖分辨率和旋转方向。
8. 捕获第一张唯一命名的截图与同名侧车，依据新截图识别真实页面。文件名、旧对话、旧坐标和检查点中的页面只能作为线索，不能作为当前状态证据。

参考命令如下；所有路径和值都必须在运行时替换：

```powershell
pwsh -NoProfile -File "<仓库目录>\scripts\resolve-mumu-target.ps1" -Action Info -VmIndex <VM索引> -RequireForeground
pwsh -NoProfile -File "<仓库目录>\scripts\capture-mumu-state.ps1" -VmIndex <VM索引> -OutputDirectory "<会话证据目录>" -RequireForeground
```

详细恢复规则见 [MuMu ADB 与恢复](references/adb-recovery.md)。

## 唯一截图和单证据单输入

每次截图必须使用新的时间戳和随机后缀，不能反复覆盖 `current.png` 一类固定名称。侧车至少包含：捕获时间、稳定标记、截图绝对位置、图像宽高、SHA-256，以及捕获前后的目标信息和上述完整指纹。截图期间指纹变化时，这份证据不稳定，不得用于输入。

每一张证据只允许消费一次，并且只允许驱动一个输入事件：一次点击、一次短而有依据的滑动，或一个未被禁止的游戏内按键事件。输入助手应在发送前重新解析目标，核对侧车年龄、截图存在性与哈希，并比较 `VM索引`、`BootId`、`AndroidId`、游戏 PID、前台包与 Activity、分辨率和旋转。证据默认只在捕获后 10 秒内有效；调用方只能把时限收紧或在 1 至 30 秒的门禁范围内调整，不能无限放宽。

首次实时指纹核验通过后，输入助手先以原子方式建立 `.consumed.json` 回执，再开始实时画面门禁：使用同一个 ADB 路径和串号取得两张唯一临时 PNG，中间保留约 180 毫秒观察窗口；随后再次解析目标，并把原证据、实时 A、实时 B 统一缩采样为固定的 `64 × 36` 网格。A/B 间的小范围动画会形成动态遮罩；遮罩外执行全局 RGB 与梯度比较，点击位置或滑动轨迹附近另做更严格的局部比较。整页转场、大弹窗、目标控件变化、动画范围过大、任一截图失败或二次指纹变化都会拒绝输入。比较门限固定在脚本中，调用方没有放宽参数；实时 B 帧从捕获到实际发送输入还必须不超过 3 秒，否则同样失败关闭并要求重新取证。

`KeyEvent` 还必须用 `-GuardX` 与 `-GuardY` 指明新截图中当前文本框或控件的位置，使允许的按键也接受局部画面门禁。回执记录视觉判定、A/B 间隔、动态占比及全局/局部 RGB、梯度和变化像素指标。无论视觉拒绝、截图失败、ADB 报错或超时，回执都保留并视为已经消费，因为检查或命令可能只完成了一部分；下一次动作必须重新截图，不能删除回执重试。远端与本地临时 PNG 在成功和失败路径都会清理。

```powershell
pwsh -NoProfile -File "<仓库目录>\scripts\send-mumu-input.ps1" -Action Tap -X <横坐标> -Y <纵坐标> -EvidenceJson "<新侧车文件>"

# 只在已核验的可见、聚焦文本框内使用允许的键码；守卫点取自同一张新截图。
pwsh -NoProfile -File "<仓库目录>\scripts\send-mumu-input.ps1" -Action KeyEvent -KeyCode 66 -GuardX <横坐标> -GuardY <纵坐标> -EvidenceJson "<新侧车文件>"
```

每次输入后只等待该过渡通常需要的短时间，然后重新解析并捕获新的唯一截图，确认预期页面确实出现后才进行下一步。禁止对一张截图批量点击、删除回执强行重试、跨分辨率复用坐标或用长时间盲等推测页面。

所有 `mumu-cli` 与 ADB 外部进程都必须通过有界执行器运行；参考实现默认单次上限为 10 秒，并允许在 100 毫秒至 120 秒范围内收紧或按环境调整。超时后必须终止本次进程树并失败关闭；输入命令超时仍可能已经部分执行，因此证据保持已消费，不能自动重试，也不能用 `adb kill-server` 影响其他实例。

只要用户报告真实画面与截图不一致，或发生超时、崩溃、手动导航、前后台切换、模型切换等变化，就立即停止所有输入；废弃旧串号、坐标、截图和页面假设，重新探测目标、核对前台与指纹并创建新的唯一截图。不得把不一致解释成延迟后继续点击。

## 研究当期活动

本工作流固化的是方法，不包含任何一期活动的固定路线、答案、坐标或战况方案。每次新活动都应：

1. 从游戏内读取活动名称、日期、版本、章节与难度、已解锁入口、地图分数或血条、活动货币、奖励与功勋状态。
2. 检索当期攻略、路线说明、敌方配置和题库，并记录来源、发布日期或编辑时间。游戏内当前条件与实际观测优先；其次采用上线后经玩家反馈修订的成熟攻略，再参考当期资料汇总和题库；早期速报只作为待核验线索。
3. 对路线、问题答案和关键机制至少做多来源核验。来源互相冲突时，结合当前游戏条件、较新完整资料与实际行为判断；仍不能确认时明确标注不确定性并暂停，而不是猜测。
4. 若资料站要求登录，只可使用用户已经登录的只读会话查看。未经明确要求，不发帖、不回复、不投票、不收藏、不关注，也不改账号设置。
5. 建立完成清单，覆盖用户要求的章节与难度、题目、战况增益、宝箱、奖励、功勋及分数阈值。

研究来源和授权边界详见 [研究、检查点与安全](references/research-safety.md)。

## 把带路条件写成出击前约束

攻略中的路线必须先转换成约束表，不能翻译成一串点击：

| 字段 | 出击前必须核验的内容 |
|---|---|
| 入口 | 仅记录可在出击前选择的 `α`、`β` 或同类入口 |
| 旗舰 | 1 号位要求的精确舰船或舰种 |
| 舰种与国籍 | 各舰种、国籍的精确数目、最小值或最大值 |
| 速度与索敌 | 最低、最高、平均或总值阈值 |
| 条件优先级 | 多项条件同时满足时哪项先判定 |
| 随机性 | 首段或后续分支是否需要退出重来或撤退 |
| 目标 | 预期到达的首领、支援或宝箱节点 |
| 解锁效果 | 通关后应开放的入口、章节或奖励 |

准备页必须逐项对照这张表。出击前只能选择游戏明确提供的入口 `α`、`β` 等；出击后显示的 `A`、`B`、`E` 等大卡片是带路条件或概率说明，不是供玩家手选的路线按钮。必须等待游戏依据舰队条件自动判路，绝不能点击 `B`、`E` 或其他节点来“选路”。走错时检查攻略版本、旗舰、舰种数量、国籍、速度、索敌、条件优先级和随机说明，再依计划撤退或重试。

## 舰队、复制人与装备

在打开选择器前先把约束翻译成六个槽位；1 号位就是旗舰，并严格区分相近舰种。

选择舰船时：

1. 先清除遗留搜索和筛选，再用右上角搜索输入精确中文显示名，同时使用舰种和国籍筛选。结果未刷新时提交软键盘。
2. 禁止靠连续滑动长列表硬找。只有搜索与筛选确实把候选缩小后，才可做一次短而有画面依据的滚动；拼音不能替代精确中文名。
3. 每次换船后返回准备页并创建新截图，复核槽位中的精确舰名、舰种、国籍、顺序和可用状态；需要时打开详情，再用新截图复核技能和装备。

同名“复制人”规则必须完整执行：

- 同名、同等级、相同伤损或相同装备都不能证明是同一艘船；名字、等级、装备、伤损和列表顺序均不能单独作为身份依据。
- 当攻略涉及技能或改造形态时，对每一个合理候选逐一核对精确技能分支或名称与等级、改造形态、舰种、装备和当前状态。若选择器不能直接显示技能，可做一次可逆选择，返回准备页打开详情核验；在核验完成前不得出击。
- 用户习惯把主力标为 `收藏`，因此同名候选应优先检查收藏者；不得把收藏与锁定标记混淆。但收藏只是一条定位信号，绝不替代技能正确性的证据。
- 选择优先级是：攻略要求的有效技能及等级、正确形态与舰种、收藏主力偏好、可用状态。技能要求与收藏冲突时，技能证据优先并向用户说明差异。
- 若无法确认收藏标记、技能、形态或候选身份，就暂停询问。绝不能因为复制人更健康、更靠前、等级相同或当前无伤而盲选。
- 伤损只表示出击准备状态，不表示身份。常规修理已获授权时，应修理技能正确的主力，而不是换成无伤但技能错误的复制人。
- 套用预设、重启恢复或用户手动改队后，必须重新执行复制人核验；六个名字相同不足以证明舰队正确。

选择装备时，进入经过核验的更换界面，使用精确中文名搜索并结合装备类型筛选，检查物品可用且适配槽位。多槽位改动前可保存装备记录；飞机尽量直接替换，避免不必要的卸下与补充。改装后返回详情或准备页，以新截图核对最终装备以及索敌、航速、制空或反潜等相关总值。

## 出击前总核验与战况中心

每次出击前必须从新截图逐项确认：

- 六个槽位的舰名、形态、舰种、国籍、顺序、旗舰与装备；所有同名或依赖技能的舰船都有精确技能名称和等级证据；
- 路线约束表中的入口、数量、速度、索敌和条件优先级；
- 补给完整，包括飞机；需要修理的正确舰船已修理，并且没有会以 `大破` 状态继续前进的舰船；
- 船坞、仓库和掉落空间没有阻塞；爆仓或满船坞在没有精确处置授权时必须暂停；
- 战况中心仍与当前舰队的主力输出类型、舰种、国籍及敌方配置相符。

浏览战况中心和拟定购买顺序是可逆的只读操作；实际购买会消耗有限活动点数。普通燃料、弹药、修理桶资源充足或一般的“资源随便用”不自动包含活动点数。购买前必须取得用户对当前增益方案或预算的明确授权；若用户已经明确授权在本次活动范围内自由购买战况增益，才可在该范围内执行。权限不清时停在购买前询问。

取得上述授权后，优先考虑任务必需的强化节点削弱，其次是主要舰种的伤害、命中与暴击，再按长路线和敌方配置考虑国籍减伤、弹药损失保护与生存。不要因为某项便宜就购买无关增益；点数不足时截图记录价格和不可用状态，采用经过说明的替代方案。舰队主要输出类型发生变化后重新打开战况中心筛选和核验。

更完整的界面操作顺序见 [活动操作](references/activity-operations.md)。

## 战斗与结算状态机

把整个游戏视为可中断状态机，至少区分：模拟器或游戏停止、登录或公告、弹窗、港口首页、活动总图、章节地图、入口选择、出击准备、舰船选择、装备选择、自动带路显示、敌方阵型、己方阵型、战斗动画、追击或夜战选择、战果评级、经验、掉落、剧情、补给与前进/回港、地图结算、奖励，以及满船坞等阻塞状态。

每个战斗节点按以下顺序处理：

1. 从新截图识别敌方舰种和阵型，再根据当前敌人、己方编成与先前记录选择己方阵型。不能把一种阵型硬编码给全部节点。
2. 战斗动画期间每隔约 1 至 5 秒用新截图轮询；短轮询优于一次长时间盲等。长战斗中按有意义的里程碑向用户报告观测事实。
3. 战果评级、经验、掉落或解锁弹窗、剧情和补给页逐屏处理，每一屏使用新的证据和一次输入。
4. 点击 `前进` 前检查每艘舰船的伤损和血条；有 `大破` 立即停止前进。路线失败、舰队不安全或攻略要求退出重来时选择游戏内 `回港`。
5. 只使用新截图中可见的游戏内箭头和关闭按钮。禁止发送 Android 返回键及其他系统导航键，以免退出或破坏流程。

一次 `SS`、`S` 或其他节点评级只证明该场战斗结果，不证明章节完成。击败首领后继续处理完整结算链，直到地图分数或血条发生预期变化、出现 `CLEAR` 标记或解锁，并检查章节和活动奖励页。只有这些持久状态与奖励领取证据齐全，才能把目标记为完成。

重复卡关时记录地图与节点、双方阵型、敌方构成、评级、剩余敌舰、己方伤损、装备和有效增益；一次只改变一个主要因素，避免用不变配置盲目重打。

## 思维殿堂与活动题库

1. 用唯一新截图捕获完整题干和全部选项。
2. 以题干中的精确、具有区分度的文字实时检索当期题库；题号和顺序不是可靠依据。
3. 用另一个当期资料源核验答案。资料冲突时优先较新、完整的题库和独立权威来源；可以查证时不得猜。
4. 只点击一次，随后以新截图确认绿色或正确结果，再把题干、选项、答案、来源和结果写入检查点。

## 中断恢复分支

发生电脑或模拟器重启、游戏或图形驱动崩溃、ADB 超时、用户手动操作、前台变化或截图不一致时，统一执行：停止输入，废弃旧证据，重新解析唯一目标，要求游戏前台，捕获唯一新截图，再按真实页面分支。

- 若仍在结算链，从当前可见的评级、经验、掉落、剧情、补给或奖励页逐屏继续。
- 若已回到首页，从活动总图和目标章节重新进入；先查地图分数或血条、`CLEAR`、解锁和奖励状态。战果已计入时不重打，只有证据显示未计入时才重新出击。
- 若回到登录、公告或弹窗，逐屏恢复到可识别的游戏内状态；不能使用旧坐标跳过。
- 若需要重建舰队，复核检查点中的槽位、形态、技能、装备、增益、补给和修理。尤其在预设、重启或用户改队后，不能只按六个名字还原。
- 若 ADB 报错或超时，视为输入可能已经部分执行；不得删除消费回执重发，必须重新取证判断结果。
- 若多个虚拟机匹配，或当前前台不是目标游戏，暂停输入直到用户确定索引或目标应用被明确恢复。

## 检查点与完成证据

在可持久恢复的里程碑后更新检查点，而不是在每帧动画后更新。至少记录：

- 活动名称、日期、目标范围与完成定义；
- `<VM索引>` 和最后核验的设备、游戏、前台、分辨率与旋转指纹；
- 最新截图和侧车、当前页面、章节、地图、入口、进度、阻塞和下一安全动作；
- 六个槽位、形态、舰种、国籍、装备、伤损；同名或技能相关舰船的收藏状态、候选数量、预期技能、实测技能与等级、证据侧车；
- 路线约束、补给与修理状态、战况中心有效增益和剩余点数；
- 已通关地图、分数或血条、`CLEAR`、解锁、题目与正确结果、奖励、宝箱和功勋；
- 研究来源、更新时间、推断和未解决的不确定性。

重启后以实时游戏状态为准，检查点只帮助定位差异。交还控制权时提供简洁交接：最后确认完成的目标、当前页面、舰队与增益及伤损、剩余清单、阻塞，以及最新截图和侧车位置。

宣布完成前，逐项取得游戏内证据：目标地图分数或血条与 `CLEAR`、章节和难度解锁、章节与活动奖励已领取、用户要求的题目/宝箱/功勋已完成，并且没有遗留结算页、爆仓或伤损阻塞。

## 不可逆操作与外部写入边界

用户授权活动代操作或说明普通资源充足时，可以按目标使用常规燃料、弹药、修理桶、补给和普通活动次数；这不等于获得任何其他不可逆授权。

未经用户针对具体对象明确授权，禁止：

- 拆解、吞噬或解锁舰船与装备，尤其是稀有、活动、高等级、已装备、收藏或锁定资产；
- 使用付费货币、扩建船坞或仓库、购买活动计划之外的物品；
- 修改账号、服务器、登录、安全或支付设置；
- 在满船坞或爆仓时自行决定清理对象；
- 让 `大破` 舰船继续前进；
- 向论坛、社交平台、邮件、文档或代码托管服务写入内容；
- 绕过前台、指纹、截图哈希、证据时效或单次消费保护。

满船坞或爆仓默认是阻塞，应请用户处理。若用户已经给出精确处置范围，也要对具体低价值目标核对两次，并保护锁定、稀有、活动、高等级和已装备资产。任何身份、资料、状态或权限无法确认时，停在可逆页面并询问。
