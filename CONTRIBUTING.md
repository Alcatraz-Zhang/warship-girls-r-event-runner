# 贡献指南

感谢你改进这套通用工作流。提交贡献即表示你同意按仓库的 [MIT 许可证](LICENSE) 提供改动。

## 开始改动

1. 从最新默认分支创建用途清晰的分支；
2. 保持每次改动小而聚焦，不把无关重排、格式化或重构混入同一提交；
3. 先阅读 [WORKFLOW.md](WORKFLOW.md)，确保改动不会产生互相冲突的第二套流程；
4. 面向人的文档使用自然中文；`scripts/*.ps1` 必须保持纯 ASCII，以兼容 Windows 环境；
5. 在提交说明中写清问题、行为变化、安全影响和验证结果。

## 本地验证

所有改动，包括只修改文档的改动，都应在仓库根目录运行：

```powershell
pwsh -NoProfile -File tests/validate-repository.ps1
pwsh -NoProfile -File tests/test-validate-repository.ps1
pwsh -NoProfile -File tests/test-mumu-safety.ps1
pwsh -NoProfile -File tests/test-send-mumu-input.ps1
```

四项验证都必须保持离线，不连接真实模拟器、不使用真实账号，也不向 ADB 目标发送输入。

## 禁止提交的内容

- 游戏截图、录屏、账号标识、登录信息、存档或运行检查点；
- Token、密码、Cookie、设备标识、未脱敏日志或用户专属绝对路径；
- 游戏客户端、APK、共享库、图片、音频、字体、Unity 资源或其他受限制的游戏资源；
- `.skill` 归档、构建产物、临时目录、依赖缓存或原始会话评测目录；
- 真实运行产生的证据侧车、消费回执或本地调试输出。

提交前应检查暂存文件和完整差异，确认示例只使用 `<仓库目录>`、`<任务工作区>` 等通用占位符或明确的环境变量。

## 活动资料变动

路线、题库和战况方案会随活动与版本变化。不要把任何一期的固定路线、答案、坐标、阵容或购买方案硬编码进这个通用仓库。若资料变化暴露出流程缺口，应改进检索、来源核验、约束表达或失败处理方法，并用不依赖真实活动数据的离线场景验证。

## 安全问题

可能造成 ADB 误目标、误输入、保护绕过或敏感信息泄露的问题不要先提交公开 Issue 或拉取请求。请按 [SECURITY.md](SECURITY.md) 的私下报告流程处理。
