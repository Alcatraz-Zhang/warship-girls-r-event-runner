# 安全政策

## 应私下报告的问题

以下情况属于本项目的安全问题：

- 动态目标解析可能选中错误的 MuMu 实例、ADB 设备、游戏进程或前台应用，从而造成误输入；
- 可以绕过前台与设备指纹核验、截图哈希或时效检查、单证据单输入限制、消费回执等保护；
- 仓库、错误输出或文档可能泄露 Token、账号资料、设备标识、Cookie、用户专属绝对路径或其他凭据；
- 失败恢复可能在目标不明、状态不明或输入结果不明时重复发送操作。

单纯的当期攻略过时或阵容建议分歧通常不是安全漏洞；如果它能导致越权写入、不可逆资产处置或绕过上述保护，则应按安全问题报告。

## 报告方式

请优先通过本仓库的 GitHub Security Advisory 私下提交报告。在 GitHub 仓库的 **Security** 页面进入 **Advisories**，选择报告漏洞，并等待维护者在私下渠道回复。

如果 Security Advisory 暂不可用，只在公开 Issue 中发布建立私下联系所需的最小、无敏感描述；不要公开漏洞细节、可直接利用的步骤或任何真实运行数据。待私下渠道建立后再补充完整信息。

绝不要在公开 Issue、讨论、拉取请求或提交中粘贴 Token、账号信息、截图、Cookie、检查点、设备串号、用户路径或含敏感内容的日志。若凭据已经暴露，应先在对应服务中撤销或轮换，而不是等待仓库修复。

## 报告内容

在不暴露真实账号和本机信息的前提下，请提供：

- 受影响的版本或提交；
- 问题类型、预期保护和实际行为；
- 最小复现条件与步骤，优先使用假 ADB、临时目录和离线证据；
- 可能影响的操作范围，以及是否涉及误目标、重复输入或数据泄露；
- 已尝试的缓解方式和可选修复建议。

请把用户名、账号、设备串号、PID、端口、绝对路径和截图内容替换为通用占位符。不要为了复现问题而连接他人的设备、使用真实付费操作或扩大现有影响。
